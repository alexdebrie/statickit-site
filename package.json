{
  "private": true,
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT"
  },
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "react-svg": "^10.0.10",
    "react-syntax-highlighter": "^11.0.1"
  },
  "devDependencies": {
    "autoprefixer": "^9.6.0",
    "cssnano": "^4.1.10",
    "postcss-easy-import": "^3.0.0",
    "tailwindcss": "^1.0.4"
  }
}
