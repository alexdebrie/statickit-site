{
  "private": true,
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT",
    "prettier": "npx prettier --write '{components,pages}/**/*.js'"
  },
  "engines": {
    "node": "10.x"
  },
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "next": "latest",
    "prismjs": "^1.16.0",
    "react": "latest",
    "react-dom": "latest",
    "react-svg": "^10.0.10"
  },
  "devDependencies": {
    "autoprefixer": "^9.6.0",
    "cssnano": "^4.1.10",
    "postcss-easy-import": "^3.0.0",
    "prettier": "^1.18.2",
    "tailwindcss": "^1.0.4"
  }
}
